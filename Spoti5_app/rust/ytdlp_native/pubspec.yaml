name: rust_lib_ytdlp_native
description: Native yt-dlp wrapper for Flutter via flutter_rust_bridge
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_rust_bridge: ^2.12.0

flutter:
  uses-material-design: true