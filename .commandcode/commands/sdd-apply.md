Cambia a la configuración de modelo para la fase de IMPLEMENTACIÓN del ciclo SDD:

1. Ejecuta `/model deepseek/deepseek-v4-flash` para usar un modelo rápido y preciso para código
2. Ejecuta `/effort medium` para esfuerzo equilibrado

Luego procede con la implementación siguiendo el spec aprobado: escribe tests primero (test-first), implementa el código, y valida con `npm run test`.
