# workflow
- For Android file recovery tasks on Mac: use ADB (Android Debug Bridge) as the primary interface for phone interaction and file transfer, preferring `adb pull` and `adb shell` commands over manual GUI operations. Confidence: 0.90
- When organizing recovered files, use a date-based folder structure (e.g., `Sep2025_plus/`) with source-based subdirectories (Camera, WhatsApp_Sent, WhatsApp_Private, WhatsApp_Video, Screenshots) for clear categorization. Confidence: 0.85
- Follow a structured plan-based recovery approach: read the plan file first, assess current state, then execute recovery steps systematically with todo tracking. Confidence: 0.85
- Prefer bulk file transfer operations (tar archives piped via ADB) over individual file `adb pull` commands for efficiency when recovering large numbers of files. Confidence: 0.80
- When a tool or download source fails (e.g., APKMirror, APKPure blocked by Cloudflare/JS), try alternative sources (F-Droid, GitHub releases, APKCombo, direct CDN links) before abandoning. Confidence: 0.75
- After completing implementations and validations, automatically create taste learnings from project decisions made during the session. Confidence: 0.85
- After running tests that required starting external services (Node.js backend, iproxy, emulators, etc.), automatically verify and stop all services that were started for those tests. Confidence: 0.90
- At end-of-session service cleanup: follow a systematic multi-step approach — first inventory all running background tasks with `shell_tasks` (includeStopped: true), kill each by its task ID via `kill_shell`, then verify ports are free with `lsof -ti:<port>` and no target processes remain with `pgrep -fl <process>`, finally cleaning up any residual/zombie processes with `pkill -f "flutter run"` / `pkill -f "dart.*snapshot run"`. Confidence: 0.85
- When testing on multiple local device targets simultaneously (e.g., iOS simulator + Mac desktop), run integration tests in parallel using background shells rather than sequentially to save time. Confidence: 0.75
- When deploying a Flutter app to multiple connected physical devices at once (e.g., iPhone via wireless + Android via USB), launch separate `flutter run --release` commands for each device in parallel background shells — builds share compilation caches so they benefit from concurrent execution, and device installs are independent. Monitor progress by tailing each shell's log output in parallel. Confidence: 0.70
- After any complex change or implementation, always persist it in Engram documenting what was done and any remaining next steps. Confidence: 0.90
- For complex tasks: Follow a structured workflow from plan to implementation step by step. Confidence: 0.85
- Before starting complex tasks: Create a new branch to avoid introducing noise into the main branch. Confidence: 0.70
- During test runs that involve external devices or services, when a repetitive error pattern emerges across attempts, proactively ask the user if they need to perform a check on the device or external application (e.g., Developer Mode, Developer Trust, USB connection, Wi-Fi) before retrying. Confidence: 0.85
- Use GitFlow with `develop` as the integration branch, `feature/*` for new features, `bugfix/*` for bug fixes, `hotfix/*` for urgent production patches, and `release/*` for release preparation; `main` is production-only. Confidence: 0.80
- After completing a phase of a roadmap/plan, always update the roadmap document to record the current phase, add notes about deviations from what was initially planned, and review for any relevant observations after updating. Use structured session entries: session header with date/emoji, numbered objectives and results with checkboxes, code blocks for logs/errors, and a conclusion paragraph. Confidence: 0.75
- When debugging on a physical device in release mode (where `kDebugMode` is false), temporarily replace guarded `print()` with unconditional prints to capture runtime logs via `flutter run`, then revert after diagnosing. Confidence: 0.80
- Before launching a new `flutter run` on a physical device, always clean up any existing Flutter processes (`pkill -f "flutter run"`, `pkill -f "flutter_tools"`) to avoid port/device conflicts. Confidence: 0.85
- When a complex problem lacks a clean solution in the current context (e.g., it depends on an external service, upstream bug, or infrastructure not available), write a comprehensive investigation report in `/docs` instead of forcing a suboptimal fix — defer resolution for later study. Confidence: 0.85
- Before resuming work on an ongoing issue, proactively check Engram for traceability of the problem — review prior observations, solutions tried, and their outcomes to maintain context continuity. Confidence: 0.85
- When preparing for a multi-phase feature: create a base feature branch from `develop` holding shared foundation (docs, config, shared code), then fork phase-specific branches from that base — this separates preparation work from implementation work and keeps each phase's commits isolated. Confidence: 0.75
- When stashing to switch branches and there are untracked documentation/asset files (e.g. new specs, non-committed docs), use `git stash -u` to include untracked files so they survive the branch switch rather than being left behind or lost. Confidence: 0.75
- After creating a multi-level branch hierarchy, verify ancestry with `git merge-base --fork-point <child> <parent>` and `git rev-list --count <parent>..<child>` to confirm the child branch forks correctly from its intended parent. Confidence: 0.70
- After completing project preparation (branch creation, docs, checklist verification), switch to and leave the repository on the branch where implementation will begin, so the user can start immediately without an extra checkout. Confidence: 0.75
- User explicitly prefers that all preparation (environment setup, branch creation, documentation, checklist verification) be fully completed before any implementation phase begins — maintain a clear boundary between prep and implementation and do not start implementation code until prep is confirmed ready. Confidence: 0.80
- When the user asks to monitor logs while they test on a physical device, proactively poll the background process output and report findings as soon as the user signals testing is done — don't wait passively or ask unnecessary questions. Confidence: 0.85
- When verifying whether a physical device's requests actually traversed a tunnel/proxy (not just local network), inspect backend logs (e.g., search queries, stream resolutions, yt-dlp activity) to confirm tunnel-originated traffic rather than relying solely on client-side behavior or tunnel-side logs. Confidence: 0.75
- Use todo lists with status tracking (pending → in_progress → completed) to manage complex multi-step testing or implementation sessions, updating each step's status as it completes so progress is always visible. Confidence: 0.75
- Start sessions by reading the project's roadmap `next-session-prompt.md` (or similar context-transition file) to understand the current phase, implemented changes, and pending work — provides continuity from the previous session's handoff. Confidence: 0.70
- When a UI behavior bug appears identically across ALL platforms (web, iOS, Android), the root cause is in shared code — start investigation on the web app (Playwright) for fast iteration and root cause analysis, then verify the fix applies cross-platform. Confidence: 0.85
- When the user gives a direct action instruction (stop a process, relaunch the app, etc.), execute it immediately without preamble, excessive explanation, or delays. The user expects prompt action, not narration. Confidence: 0.90
- In investigation reports and documentation, always distinguish verified facts from unverified assumptions — mark claims that depend on prior branch/test conditions with explicit "UNVERIFIED" or "PENDING" notes rather than stating them as established facts. Confidence: 0.85
- Use `git checkout <branch> -- <pathspec>` to selectively cherry-pick specific files/directories from another branch (e.g., docs, assets) instead of `git cherry-pick` (which operates on commits) or manual file copying — e.g. `git checkout fix/ios-C-progressive-file -- docs/`. Confidence: 0.75
- Before committing Flutter changes, run `flutter pub get` to ensure `pubspec.lock` is up to date. Confidence: 0.75
- After TypeScript backend changes, always run `npx tsc --noEmit` to verify type safety before considering changes complete. Confidence: 0.80
- Stage all changes with `git add -A` and commit using conventional commit format via heredoc (`git commit -F - <<'EOF'>`) with a detailed body (bullet-point details) and `Co-authored-by: CommandCodeBot <noreply@commandcode.ai>` trailer. Confidence: 0.75
- When auto-managed taste/skill-registry files are present in the working tree, use selective `git add <file1> <file2> ...` instead of `git add -A` to exclude those auto-managed files from the commit. Confidence: 0.80
- After committing, verify the commit with `git log --oneline -3` and `git show --stat --oneline HEAD`. Confidence: 0.75
- After committing on feature branches, push to the remote (`git push origin <branch>`) so changes are immediately available for PR creation. Confidence: 0.80
- Maintain a `next-session-prompt.md` (or similar context-transition file) in project docs to prepare context for the next session when wrapping up complex work or handing off to the next solution — include summary, decision matrix, solution objective, code changes required, test plans, and relevant file list. Confidence: 0.70
- After significant sessions, verify that Engram has captured the session observations and check for memory conflicts across related memories — the user explicitly asks for this confirmation post-session. Confidence: 0.85
- When the user explicitly says "don't solve this now, prepare context for the next session," defer all implementation and instead focus on documentation: write a comprehensive bug report, capture current code, generate root-cause hypotheses, and list investigation tasks — do NOT attempt fixes. Confidence: 0.90
- For deferred bug context: use `docs/bugs/` subdirectory (following the project's convention of thematic subdirectories under `docs/`, not a root-level `bugs/` directory) with structured markdown reports containing: Status, Summary, Environment table, Steps to Reproduce, Current Code with fix attempt, Root Cause Hypotheses, Fix History table, Investigation Tasks checklist, and Related Files. Confidence: 0.80
- When work is explicitly deferred to the next session, verify that all background services (backend, tunnels, running apps) remain active rather than stopping them — the environment must be preserved for continuity so the next session can pick up immediately. Confidence: 0.80
- Use Engram's `mem_compare` tool to check for conflicts between related memories — mark them as "related" (not "conflicting") with reasoning when they cover different phases of the same investigation. Confidence: 0.80
- When verifying project readiness before starting implementation, run multiple independent verification shell commands (git state, tool versions, file existence, branch history) in parallel rather than sequentially to quickly assess all preconditions. Confidence: 0.85
- Present pre-implementation state analysis as a structured markdown checklist table with status indicators (✅/❌) and Detalle columns, plus numbered critical findings, so the user can quickly see what is and isn't in place. Confidence: 0.80
- Use `git ls-files` to verify whether documentation, assets, or project files are tracked/committed in git before assuming they are part of the repository state. Confidence: 0.75
- Use `git log --all --oneline --graph` to trace branch history across all branches when investigating whether a branch was ever created or has prior commits referencing a feature name. Confidence: 0.75
- After presenting a state analysis and proposed plan, ask for explicit user confirmation ("¿Quieres que proceda a…?") before creating branches or making structural changes, rather than proceeding unilaterally. Confidence: 0.85
- Close Engram sessions with `mem_session_summary` (structured: Goal, Instructions, Discoveries, Accomplished, Next Steps, Relevant Files) followed by `mem_session_end`. Confidence: 0.80
- Use individual `read_file` calls instead of batch `read_multiple_files` when planning to use `edit_file` on those files — batch/multiple reads don't register for the edit tool. Confidence: 0.75
- Generated plugin files (GeneratedPluginRegistrant.swift/.cc/.cmake) auto-update with `flutter pub get` and should be committed alongside code changes — don't assume only Dart files need staging. Confidence: 0.80
- When evaluating architectural solutions, the user prefers to compare the current/working branch implementation with the `develop` branch to identify key technical differences (library choices, capabilities, tradeoffs) before deciding on an approach. Confidence: 0.65
- When merging feature branches into `develop`, use `--no-ff` to preserve merge history. Confidence: 0.75
- Before performing merge operations, verify git state (`git status`, branch listing, current branch, recent `git log`) to understand what is committed versus uncommitted in the working directory. Confidence: 0.70
- When merging with uncommitted changes present in the working directory, stash changes first, switch to the target branch, perform the merge, switch back to the original branch, and restore the stash (`git stash pop`) to avoid losing in-progress work. Confidence: 0.70
- When testing features that depend on rate-limited external APIs (e.g., YouTube CDN via yt-dlp), explicitly document and enforce testing constraints in session logs: max 2 attempts per session to avoid rate limiting, 60+ minute cooldown between intensive test sessions, and use `flutter run --debug` to capture device logs. Confidence: 0.85
- When verifying HTTP endpoints (especially through tunnels/proxies), use `curl -s -w` with `{http_code}`, `{time_total}`, and `{content_type}` format strings to capture both response correctness and performance metrics in a single command. Confidence: 0.75
- When troubleshooting performance or quality issues in optimization context docs, decompose the problem into specific numbered root causes/sources — each with file references and expected impact — before proposing solutions, rather than jumping directly to fixes. Confidence: 0.75
- Start the Node.js/TypeScript backend for local testing with `NODE_ENV=development npx tsx src/server.ts` (tsx enables runtime TypeScript execution without pre-compilation). Confidence: 0.85
- Use `python3 -c` inline scripts to parse and format JSON in shell command pipelines (e.g., processing curl API responses, extracting fields from search/track data). Confidence: 0.70
- When the user plans to resume work on a deferred/ongoing bug in a future session, they prefer to be given a ready-made structured prompt they can hand to the assistant at the start of that session — one that references the bug report by its documented file path (`docs/bugs/...`) and bundles: a concise problem description, what has already been tried, specific hypotheses to investigate, and concrete tasks to execute — rather than relying on the assistant to independently rediscover context. Confidence: 0.85

